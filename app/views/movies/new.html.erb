<div class="row">
  <div class="col-md-12 text-center">
    <%= form_for(@movie) do |f| %>
      <%= f.text_field :title %>

      <div class="p-2 mt-2">
        <%= f.submit "Chercher un film", class: "btn btn-lg btn-primary" %>
      </div>

    <% end %>
    </div>
</div>
