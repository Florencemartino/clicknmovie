<div class="row">
  <div class="col-md-12 text-center">
    <% if @movie_title %>
        toto
    <% else %>
      <div data-controller="search">
        <input data-search-target="title" type="text" >
          <button data-action="click->search#movie" class="btn btn-primary">Recherche</button>
      </div>
      <div class="col-md-12 text-center">
        <ul id="results" class="list-inline"></ul>
      </div>
    <% end %>
  </div>
</div>
